#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../winser-with-api/bin/winser" "$@"
  ret=$?
else 
  node  "$basedir/../winser-with-api/bin/winser" "$@"
  ret=$?
fi
exit $ret
